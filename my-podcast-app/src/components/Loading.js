// src/components/Loading.js

import React from 'react';
import './Loading.css'; // Import the CSS for loading animation

const Loading = () => {
    return (
        <div className="loading">
            <div className="loader"></div>
        </div>
    );
};

export default Loading;
